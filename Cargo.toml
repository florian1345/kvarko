[workspace]

members = [
    "kvarko-cli",
    "kvarko-engine",
    "kvarko-model",
    "kvarko-proc-macro",
    "kvarko-view"
]

[profile.test]
opt-level = 1

[profile.release]
opt-level = 3
lto = true
